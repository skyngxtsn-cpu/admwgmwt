# 薬局棚卸しアプリ 説明書

スマホで棚の前で使う、薬局棚卸し用のWebアプリです。

- **公開URL:** https://skyngxtsn-cpu.github.io/admwgmwt/
- **スプレッドシート:** Google Drive内「薬局棚卸し」
- **目的:** 棚卸し時の数量・期限入力 → 数量差異検出 + VAN期限との照合（期限相違検出）+ 9ヶ月以内に切れる「切迫品」を自動抽出

---

## 1. 初回セットアップ

スマホで使う場合：

1. 公開URLを Safari で開く
2. 共有 → **「ホーム画面に追加」** で擬似PWA化
3. ホーム画面のアイコンから起動できるようになる
4. 独自のスプレッドシートを使いたい場合は → **7章「新規スプレッドシートに切り替える」** 参照

---

## 2. 画面の構成

```
┌─ 全体の進捗  320 / 1858品目 (17%) ──┐
│ ████████░░░░░░░░░░░░░░               │
└──────────────────────────────────────┘

┌─ カウントモード ────────────────────┐
│ [全品目] [未実施のみ] [差異品再カウント]│
└──────────────────────────────────────┘

┌─ 棚番を選択 ──────────── 更新 ──────┐
│ A-001 ████████░░ 8/10 (80%)        │
│ A-002 ████░░░░░░ 4/10 (40%)        │
│ ...                                  │
└──────────────────────────────────────┘
```

### カウントモード
| モード | 用途 |
|---|---|
| **全品目** | 通常モード。棚内のすべての品目を表示 |
| **未実施のみ** | 今日まだカウントしていない品目だけ表示（途中再開や部分棚卸し向き） |
| **差異品再カウント** | 数量差異が出た品目だけを再カウント（最終チェック向き） |

---

## 3. 基本的な棚卸し手順

### Step 1：棚番を選ぶ
棚番一覧から、これから棚卸しする棚をタップ

### Step 2：1品ずつカードで表示される（VAN期限あり）
```
┌──────────────────────────┐
│ ← A-001    3/41品目        │  ← 戻る + 進捗
├──────────────────────────┤
│  アーチスト錠10mg            │
│  T / 第一三共               │
│  棚番: A-001    [変更]      │
├──────────────────────────┤
│  理論在庫: 120              │
│  [⚪︎ 一致] [  数量入力  ]   │
├──────────────────────────┤
│  VAN登録期限                │
│  📅 2027-09                │  ← VANから取り込んだ期限
│  [⚪︎ 一致] [違う]           │
│                            │
│  ↓「違う」を押した場合      │
│  正しい期限（YYYY-MM）      │
│  [ 2027-06  ]              │
├──────────────────────────┤
│  [ 📝 記録して次へ ]         │
│  [スキップ]                  │
└──────────────────────────┘
```

VAN期限がない品目では、従来通りの複数期限入力欄が表示されます。

### Step 3：数量を確定
- 理論在庫と一致 → **⚪︎ 一致** をタップ
- 差異あり → **数量入力欄** に実数を入力

### Step 4：期限を確認 / 入力

#### VAN期限がある品目の場合（新機能）
- VAN登録期限が上部に表示される
- 現物の期限と **一致** → **⚪︎ 一致** をタップ
- 現物の期限と **違う** → **違う** をタップ → 正しい期限を入力
  - 「違う」判定になった品目は「**期限相違あり**」シートに自動記録
  - 後日VANの期限を修正 → 再度棚卸しで「⚪︎ 一致」を押せば自動でリストから消える

#### VAN期限がない品目の場合（従来通り）
- 切迫品（9ヶ月以内）になりそうな期限だけでOK
- 複数期限がある場合は、1つ入れると次の空欄が出る → 全部入力する

### Step 5：「記録して次へ」
- スプレッドシートに記録 → 自動で次の品目カードに進む
- 差異があった品目は「差異品」シートに自動記録
- **VAN期限と違う品目は「期限相違あり」シートに自動記録**
- 切迫品（≦9ヶ月）は「切迫品」シートに自動転記

---

## 4. 棚番変更

棚番が変わった場合：

1. カード上の **[変更]** ボタンをタップ
2. 既存の棚番一覧から新しい棚番を選択
3. そのまま **「記録して次へ」** をタップ → 棚番も一緒に保存

別途「決定」を押さなくてOK。「棚番変更」シートに変更履歴が自動記録されます。

---

## 5. 部分棚卸し（重要）

「今日はA棚だけ」「明日はB棚だけ」のように、棚を分けて少しずつ棚卸しする運用です。

### 流れ

```
A棚を棚卸し → 後日 → B棚を棚卸し → ... → 全棚完了
```

### ⚠️ 重要：部分棚卸しの度に理論在庫を更新する

部分棚卸しは時間が空くため、その間に在庫が動きます。
**「次の棚を始める前に、必ずVANから最新の在庫CSVを出して取り込む」必要があります。**

#### なぜ必要か？

| 状態 | 何が起きるか |
|---|---|
| ❌ CSV取り込まずに次の棚へ | 前回取込時点の古い理論在庫と比較される → 差異が大量発生 |
| ✅ CSV取り込んでから次の棚へ | 最新の理論在庫と比較される → 正しい差異検出 |

### 手順

1. **VAN** から F メニュー在庫CSVを最新で出力
2. アプリの **「📥 VANの在庫データを取込」** ボタンをタップ
3. CSV を選択 → 「N件更新, M件追加」のトーストが出る
4. これで自店在庫が最新化される
5. 棚番選択 → カウントモードを **「未実施のみ」** に → 残りの棚を選ぶ

### 自店在庫が更新されても消えないもの

CSV取込で **上書きされない** 情報：

- ✅ 最終棚卸日（進捗バー）
- ✅ 棚卸し記録シート（履歴）
- ✅ 期限一覧/切迫品シート
- ✅ 棚番変更シート

→ **進捗は維持されたまま、理論在庫だけ最新化される** 安全な設計です。

---

## 6. 差異品の再カウント

棚卸しが一通り終わったあと、念のため数え直したい品目を再カウントするモード。

1. カウントモードで **「差異品再カウント」** を選択
2. 差異品シートに残っている品目だけが順次表示される
3. もう一度数えて記録 → 一致したら差異品シートから自動削除

---

## 7. 新規スプレッドシートに切り替える

棚卸しサイクル毎に別のスプレッドシートで履歴を残したい、または他店舗で使う時の手順。

### 前提：GAS紐付け作業は不要
アプリは URL さえあれば任意のスプレッドシートを開けます。
新規スプレッドシートに対して GAS を「紐付ける」作業は必要ありません。

### 使えるスプレッドシートの条件
- **同じ Google アカウントの Drive** に作った空のスプレッドシート
- または、その Google アカウントに **編集権限で共有された** スプレッドシート

現在のGASは実行ユーザー=自分（GAS作成者）で動くので、その人のDriveに作るのが最も簡単。

### 手順

1. https://drive.google.com を開く
2. **新規 → Google スプレッドシート → 空白** で新規作成
3. 分かりやすい名前を付ける（例：「薬局棚卸し2026下期」「A店棚卸し」）
4. アプリの右上 **⚙ ボタン** をタップ
5. URL欄に新スプレッドシートのURLを貼り付け
6. **[📋 このスプレッドシートに切替]** をタップ
7. トースト「✅ スプレッドシートを切替」が出たら成功
8. **[📥 VANの在庫データを取込]** で在庫CSVを取込 → 自店在庫シートが自動作成される
9. 棚卸し開始（他シートも記録時に自動作成される）

### 切替の確認
- ヘッダーの 📊 バッジに **新スプレッドシート名** が表示されていればOK
- 最近使ったスプレッドシートは設定モーダル下部に自動保存（5件まで）

### 他店舗への展開
1. 他店舗の担当者に **公開URL**（https://skyngxtsn-cpu.github.io/admwgmwt/）を伝える
2. 担当者は自分のGoogleアカウントで新規スプレッドシートを作成
3. アプリの ⚙ 設定 で自分のスプレッドシートURLを貼り付け
4. **注意**：スプレッドシートは **GAS作成者のGoogleアカウント（あなた）と共有** してもらう必要がある
   - スプレッドシートの「共有」ボタン → GAS作成者のメールアドレスを追加（編集権限）
   - これで GAS 側からアクセス可能になる

---

## 8. 棚卸しサイクル管理

### サイクルとは？
「2026年9月棚卸し」「2027年3月棚卸し」のように、**1回の棚卸し全体**を表す期間名。
- 実施日（8/15、9/2 など複数日に分散）と分けて管理
- 記録は全て「サイクル名」で紐付けられ、後から集計しやすい

### ヘッダの表示
```
薬局棚卸し                    ⚙
📊 薬局棚卸し2026下期
📅 [2026年9月] [変更]     ← 現在のサイクル
📆 実施日 [2026-08-15]    ← 今日（変更可能）
```

### 初回起動時
サイクル未設定なら、自動でサイクル選択ダイアログが開きます。
実施日から推奨サイクル名（実施月の翌月）を初期選択。

### サイクル切替（記録データは変えたくない時）
📅バッジをタップ → ドロップダウンで選択 → 「このサイクルに切替」

⚠️ **サイクル切替はデータをリセットしません**。単にラベルを変えるだけ。

### 新サイクル開始（リセット付き）
「⚠ 新サイクル開始（進捗リセット）」ボタン：
1. サイクル名を選択
2. 「リセット」と入力して確認
3. 進捗・差異品・期限相違ありをクリア＋新サイクル名を設定

| 消えるもの | 残るもの |
|---|---|
| 自店在庫の理論在庫（全0） | 棚卸し記録（過去の履歴、サイクル別） |
| 自店在庫の最終棚卸日（進捗0） | 期限一覧 / 切迫品シート |
| 差異品シート | 棚番変更履歴 |
| 期限相違ありシート | 自店在庫のVAN期限 |

### 実施日について
- **デフォルト：今日**
- カレンダーから変更可能（過去に遡って記録することも可能）
- 実施日はサイクルと **独立** して自由に設定できる
  - 例：サイクル「2026年9月」で、実施日「2026-08-15」に部分棚卸しを実行できる

### 各シートに「サイクル」列
記録時に自動で **サイクル名** が記録されます：
- 棚卸し記録シート（K列）
- 差異品シート（K列）
- 期限相違ありシート（L列）

→ 「2026年9月サイクルだけを集計」がフィルタで簡単にできます。

---

## 9. シートの見方

スプレッドシート「薬局棚卸し」内の各シート：

| シート | 内容 |
|---|---|
| **医薬品マスタ** | JAN/GTINマスタ（2,074件） |
| **自店在庫** | 当店の品目一覧＋理論在庫＋最終棚卸日＋VAN期限 |
| **棚卸し記録** | 全カウント履歴（記録された全エントリ） |
| **差異品** | 数量差異が出た品目（再カウントで一致したら自動削除） |
| **期限相違あり** | VAN期限と現物の実期限が違う品目（修正して再カウントで一致すれば自動削除） |
| **切迫品** | 列=期限月、下に医薬品名を縦に羅列（YYYY.MM形式） |
| **棚番変更** | 棚番変更の履歴 |

---

## 10. 困った時

### 進捗が「途中まで」のままアプリを閉じてしまった
- 同じ棚を選び直すと **「続きから再開しますか？」** のダイアログが出る
- 「はい」で続きから / 「いいえ」で最初から

### 棚卸し記録の数字を消したい
- スプレッドシートの該当行を選択して削除すれば反映される
- 切迫品シートは GAS の `Pivot_rebuildImpending` を実行で再生成

### 全体進捗が更新されない
- 棚番選択画面の右上「更新」ボタンで強制リロード

### 切迫品の表示がおかしい
- GAS のエディタで関数 `Pivot_rebuildImpending` を手動実行

---

## 11. 部分棚卸しのおすすめ運用例

```
月曜：A〜C棚（CSV取込 → 棚卸し → 帰る）
火曜：（CSV取込）→ D〜F棚（未実施モードで残棚から選ぶ）
水曜：（CSV取込）→ G〜I棚
...
金曜：（CSV取込）→ 残り全部 → 差異品再カウントモードで最終確認
```

**ポイント**：1日の作業を始める前に必ず CSV を取り込む。これさえ守れば、進捗・履歴は維持されたまま安全に部分棚卸しできます。
